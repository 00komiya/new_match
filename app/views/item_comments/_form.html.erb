<%= form_with model: [item, item_comment], local: false do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントはコチラへ", class: "w-100 comment-textarea" %>
 <%= f.submit "送信", class: "btn btn-secondary offset-11 rounded-pill" %>
<% end %>