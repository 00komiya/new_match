<div class="container">
  <div class="row mt-5">
    <div class="col-lg-6 offset-lg-3 mt-3 box">
      <h3 class="text-center border-bottom border-white pt-2">いいね一覧</h3>
        <p class="text-center">《いいねを外すとリストから削除されます》</p>
      <% @like_items.each do |item|%>
        <%= render 'likes', item: item %>
      <% end %>
    </div>
  </div>
</div>