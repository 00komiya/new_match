<% if obj.errors.any? %>
  <div id="error_explanation">
    <h5><%= pluralize(obj.errors.count, "件のエラーが発生しています") %></h5>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>